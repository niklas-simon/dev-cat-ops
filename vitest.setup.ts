import React from 'react';

// Stellt sicher, dass React global verfügbar ist
(global as any).React = React;

// Optional: Importiere zusätzliche Jest-DOM Matcher, falls benötigt
import '@testing-library/jest-dom';